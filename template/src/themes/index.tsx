import {
  darkColors,
  dimensions,
  fonts,
  fontSize,
  icons,
  lightColors,
  space
} from './global'

export const light = {
  colors: lightColors,
  fontSize,
  space,
  dimensions,
  fonts,
  icons
}

export const dark = {
  colors: darkColors,
  fontSize,
  space,
  dimensions,
  fonts,
  icons
}
