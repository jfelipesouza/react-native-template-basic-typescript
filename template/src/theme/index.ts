import { dark, light } from './colors'
import { fontsize, padding, screen } from './sizes'

const DARK_THEME = {
  colors: dark,
  fontsize,
  padding,
  screen
}

const LIGHT_THEME = {
  colors: light,
  fontsize,
  padding,
  screen
}

export { DARK_THEME, LIGHT_THEME }
